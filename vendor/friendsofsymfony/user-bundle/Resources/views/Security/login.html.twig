{% extends "::base.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block stylesheets %}
    <link href="{{ asset('Css/signing.css') }}" rel="stylesheet" xmlns="http://www.w3.org/1999/html">
{% endblock %}
{% block body %}

<div class="container">

    <div class=" col-lg-6 col-lg-offset-3  col-md-8 col-md-offset-2 col-xs-12" id="form-connection">
        <div class="container form-signin">
            <form class="form-signin" action="{{ path("fos_user_security_check") }}" method="post">
                <input type="hidden" name="_csrf_token" value="{{ csrf_token }}">

                <h2 class="form-signin-heading text-center">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</h2>

                <label for="username" class="sr-only">{{ 'security.login.username'|trans }}</label>
                <input type="text" id="unsername"  class="form-control" placeholder="{{ 'security.login.username'|trans }}"
                       autofocus="" name="_username" value="{{ last_username }}" required="required">

                <label for="password" class="sr-only"for="remember_me">{{ 'security.login.remember_me'|trans }}</label>
                <input type="password" id="password" name="_password" class="form-control" placeholder="{{  'security.login.password'|trans }}" required="required">
                {% if error %}
                    <p style="color:red">{{ error.messageKey|trans(error.messageData, 'security') }}</p>
                {% endif %}
                <div class="checkbox">
                    <label>
                        <input type="checkbox" id="remember_me" name="_remember_me" value="on">{{ 'security.login.remember_me'|trans }}
                    </label>
                </div>
                <input class="btn btn-lg btn-primary btn-block" type="submit" id="_submit" name="_submit" value="{{ 'security.login.submit'|trans }}" />
            </form>
        </div> <!-- /container -->
        <h4 class="text-center">Visiteur :</h4>
        <table class="table table-bordered">
            <thead>
            <tr>
                <th><p>Username </p></th>
                <th><p>Password</p></th>
            </tr>
            </thead>
            <tbody>
                <tr>
                    <td><p>administrateur</p></td>
                    <td><p>azer</p></td>
                </tr>
                <tr>
                    <td><p>comptable</p></td>
                    <td><p>azer</p></td>
                </tr>
                <tr>
                    <td><p>utilisateur</p></td>
                    <td><p>azer</p></td>
                </tr>
                <tr>
                    <td><p>utilisateur2</p></td>
                    <td><p>azer</p></td>
                </tr>
                <tr>
                    <td><p>utilisateur3</p></td>
                    <td><p>azer</p></td>
                </tr>
            </tbody>
        </table>
    </div>
    <div class="bandeau"></div>
    <div class="row">
        <div class="col-xs-12 text-center">
            <a class="" href="https://github.com/John-Eddy/AppliFraisAndroid">Telecharger le projet Android</a>
        </div>
    </div>
</div>
{% endblock body %}
