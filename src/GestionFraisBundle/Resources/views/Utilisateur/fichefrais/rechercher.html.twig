{% extends 'GestionFraisBundle:Utilisateur:base.html.twig' %}
{% block title %}Rechercher fiche{% endblock  %}

{%  block body %}
    <div class="container-fluid">
        <div class="col-lg-10 col-lg-offset-1 col-md-12 " id="contenu" >
            <h2 class="">Rechercher fiche</h2>
                <div class="bandeau-small"></div>

            <div class="row">
                <div class="col-lg-12">
                    {{ form_start(form, {'attr': {'class': 'form-inline'}}) }}

                    {# Les erreurs générales du formulaire. #}
                    {{ form_errors(form) }}
                    {{ form_rest(form) }}


                    {{ form_end(form) }}
                </div>
            </div>
            <div class="bandeau"></div>
            <h4>Dernieres fiches :</h4>
            {% for uneFicheFrais in lesFicheFrais %}
                <a class="lien-fiche" href="{{ path('ficheFrais_afficher', {id: uneFicheFrais.id}) }}">
                    <div class="row" id = "{{ uneFicheFrais.id }}">
                        <div class="col-sm-6">
                            <p>Mois : {{ uneFicheFrais.mois }}/{{ uneFicheFrais.annee }}</p>
                        </div>
                        <div class="col-sm-6">
                            <p>Date de modification : {{ uneFicheFrais.dateModif|date("d/m/Y") }}</p>
                        </div>
                        <div class="col-xs-12">
                            <p>Etat : {{uneFicheFrais.getIdetatfichefrais().getLibelle() }}</p>
                        </div>
                    </div>
                </a>
                <div class="bandeau-small"></div>
            {% else %}
                <p>Aucune fiches trouvées</p>
            {% endfor %}
        </div>
    </div>
{% endblock  %}
