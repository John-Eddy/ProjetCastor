{% extends '::base.html.twig' %}

{% block body -%}
    <h1>FicheFrais</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Mois</th>
                <td>{{ entity.mois }}</td>
            </tr>
            <tr>
                <th>Datemodif</th>
                <td>{{ entity.datemodif|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Montantvalide</th>
                <td>{{ entity.montantvalide }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('fichefrais') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('fichefrais_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
