{% extends '::base.html.twig' %}

{% block body -%}
    <h1>FicheFrais list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Mois</th>
                <th>Datemodif</th>
                <th>Montantvalide</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('fichefrais_show', { 'id': entity.id }) }}">{{ entity.mois }}</a></td>
                <td>{% if entity.datemodif %}{{ entity.datemodif|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.montantvalide }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('fichefrais_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('fichefrais_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('fichefrais_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
